# requirements-android.txt — Khusus Termux / Android
# Package yang butuh Rust (pydantic-core, watchfiles, uvloop, bcrypt) DIHINDARI
# karena Rust target aarch64-linux-android tidak support build dari source di Termux.

fastapi==0.115.6
uvicorn==0.34.0
jinja2==3.1.4
pydantic==1.10.18
python-multipart==0.0.18
aiosqlite==0.20.0
PyJWT==2.10.1
httpx==0.28.1
python-dotenv==1.0.1
numpy==2.5.0
Pillow==12.2.0

# NOTES:
# - uvicorn tanpa [standard]  → gak ada watchfiles/uvloop (tapi tetap jalan)
# - pydantic v1.10.18         → tanpa pydantic-core yang butuh Rust
# - passlib[bcrypt] dihapus    → password disimpan plaintext (emang gak dipakai)
